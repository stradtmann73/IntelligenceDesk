# Local Architecture Notes

This project follows the canonical architecture in:

- `C:/BMAD/_bmad-output/planning-artifacts/architecture.md`

The local copy exists only as a convenient entry point inside the renderer repo.
